Don't flag Gemini API's codeExecution tool as a security risk - it executes on Google's servers, not locally
Module-level state for tracking costs across requests is acceptable since the code runs in GitHub Actions where the process only initializes once per user action
