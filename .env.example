# Required
OPENROUTER_KEY=sk-or-v1-your-key-here
GITHUB_TOKEN=github_pat_your-token-here # populated by github actions (only used for local testing)

# Optional — enables the search_web tool (uses Gemini)
GEMINI_API_KEY=
