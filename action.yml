name: 'PR Review Agent'
description: 'AI-powered PR review agent that provides inline code review comments'
author: 'Stefan'

inputs:
  openrouter_key:
    description: 'OpenRouter API key'
    required: true
  model:
    description: 'Model to use (e.g., anthropic/claude-opus-4.6)'
    required: true
    default: 'anthropic/claude-opus-4.6'
  mcp_config:
    description: 'JSON configuration for MCP servers. DeepWiki is included by default.'
    required: false
    default: '{"servers":[{"name":"deepwiki","transport":"http","url":"https://mcp.deepwiki.com/mcp"}]}'
  recursion_limit:
    description: 'Maximum number of steps the agent can take (recursion limit)'
    required: false
    default: '100'

runs:
  using: 'node24'
  main: 'action/index.js'

branding:
  icon: 'code'
  color: 'purple'
