name: PR Review

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  call-review:
    uses: ijw-fyi/agent-pr/.github/workflows/shared_workflow.yml@master
    secrets: inherit
