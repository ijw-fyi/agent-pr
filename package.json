{
    "name": "pr-review-agent",
    "version": "1.1.0",
    "description": "AI-powered PR review agent using LangChain and OpenRouter",
    "type": "module",
    "main": "action/index.js",
    "scripts": {
        "build": "tsc && ncc build dist/index.js -o action",
        "build:preference": "tsc && ncc build dist/preference-index.js -o action-preference",
        "build:all": "npm run build && npm run build:preference",
        "dev": "tsx src/index.ts",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@actions/core": "^1.10.1",
        "@actions/github": "^6.0.0",
        "@langchain/core": "^0.3.58",
        "@langchain/langgraph": "^0.2.0",
        "@langchain/openai": "^0.3.0",
        "@modelcontextprotocol/sdk": "^1.0.0",
        "simple-git": "^3.30.0",
        "zod": "^3.23.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "@vercel/ncc": "^0.38.0",
        "tsx": "^4.0.0",
        "typescript": "^5.0.0"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}
